pragma No_Body;

--  package body SimAVR_MCU_Section is

   --  procedure Initialize_AVR_MCU_Data is
   --  begin

   --     MMCU_String_Instance :=
   --           (Tag => MMCU_Tag'Pos (Name),
   --           Len => Nat8 (MMCU_String_T'Size / 8 - 2),
   --           Str => MCU_Name);

   --     MMCU_Long_Instance :=
   --        (Tag => MMCU_Tag'Pos (Frequency),
   --           Len => Nat8 (MMCU_Long_T'Size / 8 - 2),
   --           Val => Freq);

   --     MMCU := (MMCU_Tag'Pos (Tag), 0);
   --  end Initialize_AVR_MCU_Data;

--  private
--  end SimAVR_MCU_Section;